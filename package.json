{
  "name": "guitar-app",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "npm run dev --workspace=@guitar-app/web",
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces"
  }
}
